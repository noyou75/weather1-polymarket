# Weather1 Frontend Environment Variables
# Copy to .env.local for local overrides (never commit .env.local)

# Backend API base URL
# Local development: leave empty (Next.js rewrites /api/* to localhost:8000)
# Production (Vercel): set to your Railway backend URL
NEXT_PUBLIC_API_BASE_URL=https://weather1-polymarket-production.up.railway.app
